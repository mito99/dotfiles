[user]
	email = mito@live.jp
	name = mito99
[init]
	defaultBranch = main
[core]
	editor = nvim
	quotepath = false
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[include]
    path = ~/.gitconfig.local
